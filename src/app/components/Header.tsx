// import DesktopNavigation from './DesktopNavigation'; // Now rendered in sidebar
// import MobileNavigation from './MobileNavigation'; // Now rendered at page level

export default function Header() {
  return (
    <>
      {/* MobileNavigation now rendered at page level as floating component */}
      {/* DesktopNavigation now rendered in ContentZone sidebar */}
    </>
  );
}